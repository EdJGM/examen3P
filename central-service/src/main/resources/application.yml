server:
  port: 8080

spring:
  application:
    name: central-service

  datasource:
    url: jdbc:postgresql://localhost:5432/agroflow_central
    username: postgres
    password: Edgar123456/
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin

logging:
  level:
    com.agroflow: DEBUG
    org.springframework.web: DEBUG