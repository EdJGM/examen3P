server:
  port: 8082

spring:
  application:
    name: facturacion-service

  datasource:
    url: jdbc:postgresql://localhost:5432/agroflow_facturacion
    username: postgres
    password: Edgar123456/
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin

central-service:
  url: http://localhost:8080

logging:
  level:
    com.agroflow: DEBUG
    org.springframework.web: DEBUG